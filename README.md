Apps that will read from the Databricks Bakebouse database. 

To use the integration, environment variable named: DATABRICKS_PATH must be created. 

To find the path, login to Databricks, go to SQL Warehouse -> Connection Details Tab -> HTTP path
